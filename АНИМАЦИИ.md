# Анимации «5 букв»

Документ описывает анимации приложения: стадии, тайминги, easing и параметры трансформаций.  
Источники: `app.js`, `styles.css`.

**Версия с понятными названиями элементов** (1 в 1 по структуре и параметрам): [`АНИМАЦИИ-интерфейс.md`](АНИМАЦИИ-интерфейс.md).

---

## 1. Сводка констант (app.js)

### 1.1. Игра и экран результата

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `FLIP_STAGGER_MS` | 150 ms | Задержка между переворотами ячеек в строке |
| `FLIP_DURATION_MS` | 750 ms | Длительность переворота ячейки (игра) |
| `NORMAL_WORD_MS` | 1000 ms | Переход к экрану результата (под сплешем) |
| `WIN_HERO_MS` | 800 ms | Появление/скрытие hero-блока |
| `REVERSE_NORMAL_WORD_MS` | 1000 ms | Возврат к игровому экрану |
| `MINI_UNFLIP_FLIP_MS` | 360 ms | Переворот ячейки на миниполе («Играть ещё») |
| `MINI_UNFLIP_WAVE_COUNT` | 10 | Число диагональных волн (ROWS + COLS − 1) |
| `MINI_UNFLIP_WAVE_STAGGER_MS` | ~48.9 ms | Пауза между волнами: (800 − 360) / 9 |
| `WIN_SPLASH_FADE_MS` | 600 ms | Fade-in / fade-out фрейма сплеша (`ease-in`) |
| `WIN_SPLASH_CONTENT_DELAY_MS` | 200 ms | Пауза после fade-in фрейма до контента |
| `WIN_SPLASH_CONTENT_MS` | 700 ms | Появление / масштаб контента сплеша |
| `WIN_SPLASH_CONFETTI_AT` | 0.4 | Доля `CONTENT_MS`, на которой стартует конфетти |
| `WIN_SPLASH_TITLE_AT` | 0.6 | Доля `CONTENT_MS`, на которой стартуют заголовок и подзаголовок |
| `WIN_SPLASH_TITLE_MS` | 700 ms | Появление заголовка / подзаголовка (победа) |
| `WIN_SPLASH_CONTENT_OUT_MS` | 700 ms | Исчезновение контента сплеша («Пропустить») |
| `WIN_SPLASH_THEMATIC_DELAY_MS` | 400 ms | Пауза на пустом фрейме перед тематическим сплешем |
| `LOSE_SPLASH_FLIP_DELAY_MS` | 100 ms | Пауза перед переворотом ответа на сплеше поражения |
| `PROGRESS_PHASE1_MS` | 280 ms | Заполнение линии между бейджами |
| `PROGRESS_PHASE2_MS` | 420 ms | Заполнение бейджа |
| `PROGRESS_PHASE3_MS` | 380 ms | Заполнение линии после бейджа |
| `PROGRESS_WORD_DOTS_AT` | 0.05 | Старт анимации точек (доля от total) |
| `PROGRESS_WORD_DOTS_FOR` | 0.25 | Длительность точек (доля от total) |
| `PROGRESS_WORD_DONE_AT` | 0.95 | Конец появления слова (доля от total) |
| `PRIZE_TO_TAB_MS` | 1200 ms | Полёт приза / монет к цели |
| `PROGRESS_SWAP_MS` | 1200 ms | Смена панели прогресса (сценарий «5 слово») |
| `FIFTH_WORD_PRIZE_LINE_MS` | 420 ms | Заполнение линии к призу (5 слово) |
| `PRIZE_FLY_DELAY_MS` | 1000 ms | Пауза перед полётом приза |
| `PRIZE_POP_SCALE` | 1.2 | Масштаб приза при pop |
| `PRIZE_REVEAL_MS` | 700 ms | Появление приза на новой панели |
| `PRIZE_WIGGLE_INTERVAL_MS` | 7000 ms | Интервал idle-покачивания приза |
| `RESULT_SHEET_OPEN_MS` | 500 ms | Открытие шторы результата |
| `RESULT_SHEET_CLOSE_MS` | 300 ms | Закрытие шторы |
| `CONFETTI_COUNT` | 200 | Число частиц конфетти |

### 1.2. Призы — карусель карточек розыгрыша и intro

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `RAFFLE_CARD_WIDTH` | 270 px | Ширина карточки |
| `RAFFLE_CARD_HEIGHT` | 388 px | Высота карточки |
| `RAFFLE_CARD_GAP` | 20 px | Зазор между карточками |
| `RAFFLE_SLIDE_STEP` | 290 px | Шаг слайда (width + gap) |
| `RAFFLE_SLIDE_INDICES` | `[0, 1, 2]` | Три слайда **без зацикливания** (края не перелистываются) |
| `RAFFLE_INTRO_ENTRY_DELAY_MS` | 400 ms | Задержка перед intro |
| `RAFFLE_INTRO_STAGE1_MS` | 900 ms | Intro: сборка к центру |
| `RAFFLE_INTRO_PAUSE_MS` | 400 ms | Intro: пауза между стадиями |
| `RAFFLE_INTRO_STAGE2_MS` | 1200 ms | Intro: раскладка карусели |
| `RAFFLE_PARTICIPATE_SCALE` | 1.05 | Масштаб карточки при участии |
| `RAFFLE_PARTICIPATE_SCALE_DELAY_MS` | 200 ms | Задержка перед scale |
| `RAFFLE_PARTICIPATE_SCALE_MS` | 600 ms | Анимация scale/выравнивания |
| `RAFFLE_PARTICIPATE_FLIP_SLOW_MS` | 500 ms | Первый пол-оборот (0° → 180°) |
| `RAFFLE_PARTICIPATE_FLIP_FAST_MS` | 1200 ms | Второй пол-оборот (180° → 360°) |
| `RAFFLE_PARTICIPATE_PAUSE_MS` | 400 ms | Пауза перед возвратом |
| `RAFFLE_PARTICIPATE_RETURN_MS` | 300 ms | Возврат карточки в позицию карусели |
| `RAFFLE_PARTICIPATE_SPARKS_AT_MS` | 150 ms | Искры (50% от return) |
| `RAFFLE_BTN_TRANSITION_MS` | 400 ms | Смена иконки кнопки «Участвовать» |
| `RAFFLE_CENTER_ROTATIONS` | [+1°, −2°, 0°] | Поворот центральных карточек |
| `RAFFLE_TOKEN_SRC` | `assets/Монетка активная.png` | Иконка монеты на активной кнопке |
| `RAFFLE_TOKEN_INACTIVE_SRC` | `assets/Монетка неактивная.png` | Иконка на кнопке при нехватке монет |
| `COIN_FLY_SRC` | `assets/badge-coin.png?v=2` | Иконка летящих монет / бейджа |

**Геометрия блока карусели (CSS):**

| Элемент | Размер / позиция |
|---------|------------------|
| `.raffle-cards-block` | height **494 px**, padding-bottom **16 px**; на вкладке «Призы» → «Можно выиграть» |
| `.raffle-cards-carousel`, `.raffle-cards-track` | height **486 px**; `touch-action: pan-x pan-y` |
| `.raffle-cards-pagination` | `bottom: 16 px` |
| `.raffle-cards-pagination-label` | `bottom: 42 px` (18 px над пагинацией) |

**Старт / края:** `scrollToSlide(0)` — первая карточка; индекс слайда clamped в `[0…2]`, без бесконечного лупа.

### 1.3. Монеты и задания

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `INITIAL_COIN_BALANCE` | 0 | Стартовый баланс монет |
| `WORD_GUESS_COIN_REWARD` | 10 | Награда за угаданное слово (монеты начисляются всегда) |
| `TASK_REWARD_COIN_AMOUNT` | 250 | Награда за задание №2 |
| `COIN_FLY_SIZE` | 24 px | Размер летящей монеты |
| `COIN_COUNTER_START_PROGRESS` | 0.8 | Старт счётчика на 80% полёта первой монеты |
| `TASK_COIN_BURST_MS` | 350 ms | Фаза разлёта монет от источника |
| `TASK_COIN_BURST_DISTANCE` | 36 px | Базовый радиус разлёта (×0.85–1.2 jitter) |
| `TASK_COIN_COUNT_MIN` / `MAX` | 5 / 7 | Число монет в полёте |
| `TASK_SWAP_MS` | 800 ms | FLIP-перестановка карточек заданий |
| `TASK_BADGE_MS` | 400 ms | Смена бейджа в шторе задания |
| `TASK_CLAIM_MS` | 300 ms | Исчезновение карточки при «Забрать награду» |
| `TASK_CLAIM_REORDER_MS` | 300 ms | Схлопывание `<li>` в списке заданий |
| `TASK_STUB_FADE_MS` | 300 ms | Fade заглушки «Выполнить» |
| `TASK_STUB_HOLD_MS` | 1200 ms | Пауза с картинкой заглушки |
| `TASK_STUB_ACTION_DELAY_MS` | 300 ms | Задержка перед закрытием шторы и активацией награды |

**Производные тайминги полёта монет:**

- Полный полёт: `TASK_COIN_BURST_MS + PRIZE_TO_TAB_MS` = **1550 ms**
- Середина полёта (`halfway`): **775 ms** — старт удаления карточки задания
- Старт счётчика: burst + `COIN_COUNTER_START_PROGRESS × PRIZE_TO_TAB_MS` = 350 + 960 = **1310 ms** (первая монета)
- Счётчик баланса (`animateCoinBadgeValue`): **800 ms**, easing `1 − (1−t)³`

---

## 2. Игровое поле

### 2.1. Ввод буквы — `cell-pop`

- **Триггер:** класс `.cell.filled` на ячейке с буквой.
- **Длительность:** 100 ms, `ease`.
- **Ключевые кадры:** scale 1 → 1.04 (50%) → 1.

### 2.2. Переворот ячейки при проверке слова — `revealRow()`

**Стадии (на каждую ячейку в строке):**

1. Задержка старта: `col × 150 ms` (слева направо).
2. Добавление класса `.flipped` на `.cell-inner`.
3. CSS-transition: **750 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`, `rotateY(180deg)`.
4. После завершения всех ячеек строки — обновление клавиатуры + **120 ms** пауза до разблокировки ввода.

### 2.3. Ошибка дубликата слова — `shake`

- **Триггер:** класс `.grid-row.shake`.
- **Длительность:** 600 ms, `ease`.
- **Движение:** `translateX` −4px / +4px (чередование).
- Сообщение об ошибке снимается через **600 ms**.

### 2.4. Победа в строке — `cell-win-scale`

- **Триггер:** класс `.cell.win-scale` на ячейках выигрышной строки.
- **Длительность:** 1000 ms, `ease`.
- **Задержка:** `calc(var(--win-col) × 100 ms)` — волна слева направо.
- **Ключевые кадры:** 1 → 1.15 (30%) → 1.05 (40%) → 1.1 (50%) → 1.
- **Общая длительность JS-ожидания:** `(COLS − 1) × 100 + 1000` = **1400 ms**.

### 2.5. Tooltip дубликата

- Появление/скрытие: **200 ms**, `opacity` + `visibility` + `transform`.
- Автоскрытие: **1800 ms** (по умолчанию).

---

## 3. Клавиатура

### 3.1. Смена состояния клавиши

- `background`, `border-color`: **380 ms** `ease-out`.
- `color`: **200 ms** `ease-out`.

### 3.2. Нажатие

- `transform: scale(0.96)` — **80 ms** (мгновенный active).

---

## 4. Экран результата и сплеши

### 4.1. Общая последовательность

**Победа:**

```
revealRow → playWinScaleAnimation (1400 ms)
→ playWinSplashAnimation (фрейм + контент; под сплешем — playNormalWordAnimation 1000 ms)
→ [если «Тематическое слово»] playThematicSplashAnimation
→ fade-out фрейма сплеша (600 ms)
→ playWinHeroAnimation (800 ms, параллельно с прогрессом)
```

**Поражение (6 попыток):**

```
revealRow → playLoseSplashAnimation (фрейм + контент + переворот ответа;
  под сплешем — playNormalWordAnimation 1000 ms)
→ [если «Тематическое слово»] playThematicSplashAnimation
→ fade-out фрейма сплеша (600 ms)
→ playWinHeroAnimation (800 ms, без слова / прогресса слова)
```

**Общий easing контента сплешей:** `cubic-bezier(0.2, 0, 0.4, 0.9)`.  
**Фрейм сплеша:** `#win-splash`, от верхней границы `.nav-row` до низа, фон `#1C1C1E`.  
Кнопка «Закрыть» остаётся над сплешем и кликабельна.

---

### 4.2. Сплеш победы — `playWinSplashAnimation()`

**Классы режима:** `.win-splash--win`, стадии `.is-fading-in` / `.is-content-in` / `.is-title-in` / `.is-mini-glow` / `.is-content-out` / `.is-fading-out`.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Fade-in фрейма | 600 ms | `ease-in` | opacity фрейма 0 → 1 |
| Пауза | 200 ms | — | Пустой фрейм |
| Content-in | 700 ms | bezier | Мини-поле: opacity 0→1, `scale(1.1→1.0)` от центра (только `.win-splash__grid`, на layout не влияет). Кнопки: opacity 0→1. Параллельно `playNormalWordAnimation()` под сплешем |
| Confetti | старт на **40%** content-in (= 280 ms) | — | `playSplashConfetti()` |
| Title-in | старт на **60%** content-in (= 420 ms) | 700 ms, bezier | Заголовок / подзаголовок: opacity 0→1; `padding-top` 60→32 px; margin подзаголовка 32→8 px |
| Glow | по окончании `NORMAL_WORD_MS` | — | `.is-mini-glow`: `box-shadow` на жёлтых ячейках мини-поля |
| Content-out («Пропустить») | 700 ms | bezier | Контент opacity 1→0; отступы обратно 32→60 / 8→32; glow гаснет 700 ms; мини-поле opacity 1→0 |
| Fade-out фрейма | 600 ms | `ease-in` | Если нет тематического сплеша — сразу; иначе после него |

**Ранний «Пропустить»** (до конца title-in): без тематического слова — сразу fade-out фрейма 600 ms; с тематическим — сначала content-out (если контент уже был), затем тематический сплеш.

**Мини-поле на сплеше:** копия доски, контейнер 160 px; пустые и `absent` — `rgba(255,255,255,0.1)` + `backdrop-filter: blur(4px)` с появления.

**Конфетти сплеша:** те же 200 частиц; точки спавна смещены вниз на **20%** высоты контейнера (`spawnYShiftRatio: 0.2`).

---

### 4.3. Сплеш поражения — `playLoseSplashAnimation()`

**Классы режима:** `.win-splash--lose`. Без конфетти.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Fade-in фрейма | 600 ms | `ease-in` | opacity 0 → 1 |
| Пауза | 200 ms | — | Пустой фрейм |
| Content-in | 700 ms | bezier | Заголовок «Не отгадали слово», подзаголовок «Мы загадали:», ряд из 5 ячеек (1:1, gap 6 px, поля 52 px), «Коснитесь, чтобы продолжить». Параллельно `playNormalWordAnimation()` |
| Flip ответа | старт через 100 ms после content-in | как `revealRow` | `col × 150 ms` + flip **750 ms**; все ячейки `correct` (жёлтые) с буквами `ANSWER` |
| Content-out (тап в любую зону) | 700 ms | bezier | opacity контента 1 → 0 |
| Fade-out фрейма | 600 ms | `ease-in` | Или тематический сплеш, затем fade-out |

**Ранний тап:** та же логика, что ранний Skip на победе (см. §4.2).

---

### 4.4. Сплеш тематического слова — `playThematicSplashAnimation()`

**Когда:** после content-out победы/поражения, если включён сценарий «Тематическое слово» (в т.ч. после раннего Skip).  
**Класс режима:** `.win-splash--thematic`.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Пауза на фрейме | 400 ms | — | Контент победы/поражения уже скрыт |
| Content-in | 700 ms | bezier | Body opacity 0→1; изображение `scale(0.9→1.0)` от центра (только картинка) |
| Content-out («Пропустить») | 700 ms | bezier | Body opacity 1 → 0 |
| Fade-out фрейма | 600 ms | `ease-in` | Затем `playWinHeroAnimation` |

Кнопка «Открыть вклад» — без действия. Тексты фиксированные («ВКЛАД», подзаголовок, CTA).

---

### 4.5. Переход к результату — `playNormalWordAnimation()`

**Длительность:** `NORMAL_WORD_MS` = **1000 ms**.  
**Класс:** `.is-animating-normal-word` → затем `.scenario-normal-word`.  
**Когда:** параллельно content-in сплеша победы/поражения (под оверлеем).

| Элемент | Свойство | Длительность | Easing |
|---------|----------|--------------|--------|
| `.grid-area` | `margin-top` | 1000 ms | `cubic-bezier(0.45, 1.45, 0.8, 1)` |
| `.grid`, `.grid-row` | `gap` | 1000 ms | то же |
| `.cell` | `width`, `height` | 1000 ms | то же |
| `.cell-front`, `.cell-back` | `font-size`, `border-radius`, `color`, `background` | 1000 ms | то же |
| `.win-panel` | `transform` (slide up) | 1000 ms | то же |
| `.keyboard` | `transform`, `opacity` | 1000 ms / 600 ms | то же |
| `.win-blocks` | `opacity` | 1000 ms | то же |

**Параллельно:**

- Клавиатура уезжает вниз (`--kb-hide-y`).
- Панель победы поднимается с `--win-panel-offset`.
- Сетка сжимается до миниполя: ячейки **23×22 px**, gap **4 px**, буквы `color: transparent`.

---

### 4.6. Hero-блок — `playWinHeroAnimation()`

**Длительность:** `WIN_HERO_MS` = **800 ms**.  
**Классы:** `.is-animating-win-hero` → `.scenario-win-hero`.  
**Когда:** сразу после fade-out фрейма сплеша.

| Элемент | Свойство | Длительность | Easing |
|---------|----------|--------------|--------|
| `.win-word`, `.win-message` | `margin-top`, `opacity` | 800 ms | `cubic-bezier(0.45, 1.45, 0.8, 1)` |

Параллельно запускается `animateWinProgress()` или `animateFifthWordProgress()` (при победе со словом).

---

### 4.7. Конфетти — `playConfetti()` / `playSplashConfetti()`

- **Частиц:** 200.
- **Цвета:** `#ffdd2d`, `#19b1ff`, `#7d3bc0`, `#ffb400`.
- **Физика (на кадр):** скорость 4.5–9.5, гравитация 0.11–0.18, decay 0.005–0.009.
- **Рендер:** `requestAnimationFrame`, без фиксированной длительности.
- **Сплеш победы:** `playSplashConfetti()` — в контейнере `#win-splash-confetti`, спавн ниже на 20% высоты; старт на **40%** content-in.
- **Сплеш поражения / тематический:** конфетти нет.

---

### 4.8. «Играть ещё» — обратные анимации

#### 4.8.1. `playReverseWinHeroAnimation()` — 800 ms

1. Скролл `main` наверх за **800 ms** (easing: `1 − (1−t)³`).
2. Скрытие hero: снятие `.scenario-win-hero`, класс `.is-animating-reverse-win-hero`.
3. **Unflip миниполя** — `unflipAllCells()` (см. §4.9).

#### 4.8.2. `playReverseNormalWordAnimation()` — 1000 ms

**Классы:** `.is-animating-reverse-normal-word` → `.is-returning-to-game`.

| Элемент | Easing |
|---------|--------|
| Сетка, ячейки, панель, клавиатура, блоки | `cubic-bezier(0.7, 0, 0.3, 1)`, **1000 ms** |

---

### 4.9. Unflip миниполя — `unflipAllCells()`

**Когда:** нажатие «Играть ещё», внутри `playReverseWinHeroAnimation`.  
**Общая длительность:** **800 ms**.

**Порядок:** диагональные волны (10 волн, `ROWS + COLS − 1`).

**Параметры:**

- Старт волны `d`: `round(d × 48.89 ms)`.
- Переворот одной ячейки: **360 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`.
- CSS: `.scenario-normal-word.is-animating-reverse-win-hero .cell-inner`, переменная `--mini-unflip-ms`.

---

## 5. Прогресс победы

### 5.1. Общая длительность фаз по слоту

| Слот | Phase 1 | Phase 2 | Phase 3 | **Total** |
|------|---------|---------|---------|-----------|
| 0 | — | 420 ms | 380 ms | **800 ms** |
| 1–3 | 280 ms | 420 ms | 380 ms | **1080 ms** |

### 5.2. Фазы `animateWinProgress(word)`

1. **Phase 1** (слоты 1–3): линия до бейджа → 100%, **280 ms**.
2. **Phase 2:** заливка бейджа (scaleX 0→1), **420 ms**. Опционально: стикер тематического слова; при включённом разделе розыгрышей — полёт монет + счётчик **+10**.
3. **Phase 3:** линия после бейджа → 50%, **380 ms**.

**CSS transitions при `.is-transitioning`:**

| Элемент | Быстрый | Медленный (`.is-transitioning--slow`) |
|---------|---------|---------------------------------------|
| `.win-progress__line-fill` | 280 ms | 380 ms |
| `.win-progress__badge-fill` | 280 ms | — |
| `.win-progress__badge-text` (color) | 420 ms | — |
| `.win-progress__prize-anchor-fill` | 280 ms / 420 ms | — |

Easing фаз прогресса: `cubic-bezier(0.45, 1.45, 0.8, 1)`.

### 5.3. Награда монетами за слово — `triggerWinProgressCoinReward()`

**Условие:** всегда (вкладка «Задания» и бейдж монет не зависят от «Активная стадия»).

1. `runCoinsToBadgeAnimation()` от бейджа прогресса (или prize anchor в сценарии «5 слово»).
2. После `firstArrival` (80% полёта первой монеты) — `animateCoinBadgeValue` на **+10** за **800 ms**.
3. Параллельно с phase 2/3 прогресса, не блокирует анимацию полосы.

### 5.4. Анимация слова под бейджем

- **Точки:** старт на **5%** total, длительность **25%** total.
- **Слово:** старт после точек, видимость до **95%** total.
- Easing: `cubic-bezier(0.45, 1.45, 0.8, 1)`.
- Начальное состояние: `opacity: 0`, `scale(0.8)`.

### 5.5. Тематический стикер — `thematic-sticker-attach`

- **420 ms**, `cubic-bezier(0.45, 1.45, 0.8, 1)`.
- От `opacity: 0`, `scale(0.35)`, `rotate(-14°)` → к финальному состоянию.

### 5.6. Приз на прогресс-баре

#### Появление (reveal)

- **700 ms**, `cubic-bezier(0.33, 1, 0.68, 1)`.
- `opacity: 0 → 1`, `scale: 0.5 → 1`.

#### Pop (пятисловный сценарий)

- Класс `.is-popped`: **scale 1.2**, **420 ms**, `cubic-bezier(0.33, 1, 0.68, 1)`.
- Частицы: `spawnPrizeParticles(el, 1.7)`.

#### Idle-покачивание — `wigglePrize()`

- Интервал: **7000 ms**.
- Анимация: **500 ms**, `ease-in-out`.

#### Полёт к табу «Подарки» — `flyPrizeToGiftIcon()`

- **1200 ms**, квадратичная Bézier.
- **Scale:** 1.2 → 0.5.
- **Opacity:** 1 до 40% пути, затем fade (`flyOpacity`).
- Easing позиции: `1 − (1−t)³`.
- Иконка таба: `gift-prize-landing`, **480 ms**, срабатывает на **60%** полёта.

### 5.7. Сценарий «5 слово» — `animateFifthWordProgress()`

1. Анимация слова «ВКЛАД» в колонке приза.
2. Линия 3 → 100% (**420 ms** slow).
3. Коннектор → 100%, anchor fill → 1.
4. Полёт монет **+10** от prize anchor (как в §5.3).
5. Pop приза + частицы.
6. Пауза **1000 ms** → полёт к табу (**1200 ms**).
7. Смена панели (**1200 ms**) → reveal (**700 ms**).

---

## 6. Штора результата

### 6.1. Открытие

- Панель: `translateY(100%) → 0`, **500 ms**, `cubic-bezier(0.4, 0.1, 0.2, 1)`.
- Подложка: `opacity 0 → 1`, **500 ms**.

### 6.2. Закрытие (крестик / drag)

- **300 ms**, `cubic-bezier(0.4, 0, 1, 1)`.
- Класс `.is-closing`.

### 6.3. Drag-to-close

- Порог: **25%** высоты панели.

Штора задания (`#task-sheet`) использует те же `RESULT_SHEET_OPEN_MS` / `RESULT_SHEET_CLOSE_MS` и ту же механику drag.

---

## 7. Карусель призов (вкладка «Призы» → «Можно выиграть»)

Карусель живёт на экране призов. Переключатель профиля **«Активная стадия»**: вкл. — карусель; выкл. — заглушка Hero (вкладка «Задания» и монеты остаются).

Intro и участие запускаются при открытии вкладки «Призы», если стадия активна.

### 7.1. Карусель — `updateCardTransforms()`

Интерполяция по `progress` = `min(1, |offset| / RAFFLE_SLIDE_STEP)` (0 = центр, 1 = бок):

| Параметр | Центр (0) | Бок (1) |
|----------|-----------|---------|
| `scale` (wrap) | 1 | 0.9 |
| `translateY` | 0 px | 48 px |
| `opacity` | 1 | 0.75 |
| `rotate` | centerRotate × (1−p) + offset/step × 4° | — |
| `visual scale` | 1 | 0.9 |

**centerRotate:** `[+1°, −2°, 0°]` по `data-card-index`.

Обновление: `scroll` (RAF) + `scrollend` (debounce **120 ms**). Слайды `0…2` без лупа.

### 7.2. Подпись над пагинацией — `updateRafflePaginationLabelOpacity()`

Текст «Подведём итоги 1 декабря» (`.raffle-cards-pagination-label`):

```
distanceFromSnap = |scrollProgress − round(scrollProgress)|
opacity = 1 − min(1, distanceFromSnap × 2)
```

- Карточка в центре snap → **opacity 1**.
- Между snap-позициями (середина скролла) → **opacity 0**.
- Пересчёт на каждом кадре скролла (без CSS-transition).
- Во время intro (`is-intro-active`, `raffleIntroRunning`) — управляется отдельно (см. §7.4).

### 7.3. Переворот карточки тапом — `toggleRaffleCardFlip()`

- **750 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`.
- Накопительный `rotateY` (+180° за тап).

### 7.4. Участие в розыгрыше — `runRaffleParticipate()`

| # | Стадия | Длительность | Easing / параметры |
|---|--------|--------------|-------------------|
| 1 | Задержка | 200 ms | — |
| 2 | Scale + выравнивание | 600 ms | `cubic-bezier(0, 0, 0.4, 0.9)`, scale **1.05** |
| 3 | Flip 0° → 180° | 500 ms | `cubic-bezier(0.4, 0, 1, 1)` |
| 4 | Смена контента | — | — |
| 5 | Flip 180° → 360° | 1200 ms | `cubic-bezier(0.4, 0.1, 0.2, 1)` |
| 6 | Пауза | 400 ms | — |
| 7 | Возврат в карусель | 300 ms | `cubic-bezier(0.45, 1.45, 0.48, 1)` |
| 8 | Искры | 150 ms от старта return | `particleScale: 4.05`, sparkOnly |

**Параллельно со стадией 1:** списание монет и `animateCoinBadgeValue` (**800 ms**, без ожидания).

**Кнопка «Участвовать»:** иконки `Монетка активная` / `Монетка неактивная`; смена — **400 ms**, opacity crossfade через **200 ms**. Во время intro кнопки тоже отражают баланс (`updateRaffleParticipateButtonsState`).

### 7.5. Intro при первом входе — `runRaffleIntroAnimation()`

**Задержка:** **400 ms**. Старт измерения и финальной раскладки — слайд **0** (первая карточка).

**Начальное состояние:** центр экрана, `scale: 0.8`, `opacity: 0`; карточка 0 — flip **180°**.

#### Стадия 1 — 900 ms

- Easing: `cubic-bezier(0, 0, 0.4, 0.9)`.
- Карточка 0: `opacity → 1`, flip → 0°.

#### Пауза — 400 ms

#### Стадия 2 — 1200 ms

- Easing: `cubic-bezier(0.35, 1.3, 0.25, 1)`.
- Раскладка в позиции карусели (слайды 0–2 без клонов).
- **Пагинация и подпись** (`raffle-cards-pagination`, `raffle-cards-pagination-label`): `opacity 0 → 1`, transition **1200 ms**, тот же easing.

**Итого:** 400 + 900 + 400 + 1200 = **2900 ms**.

### 7.6. Бегущая полоса — `#raffle-page-footer`

- Переключатель «Бегущая полоса» в профиле (по умолчанию **выкл.**).
- Футер на экране вкладки «Задания» (`#raffle-screen`).
- При включении — показ `.raffle-page-footer`; при выключении — `hidden` + `display: none !important`.
- Статичная подпись над пагинацией (§7.2) от свича не зависит.

### 7.7. Shimmer на кнопке «Участвовать»

- **380 ms**, `ease-out`.
- Градиентная полоса: `translate(-55%, 55%)` → `translate(55%, -55%)`.

---

## 8. Задания (вкладка «Задания»)

### 8.1. Перестановка карточек — `animateTaskCardsSwap()`

- FLIP: начальный `translateY(delta)` без transition → сброс transform.
- Transition: **800 ms**, `cubic-bezier(0.4, 0.1, 0.2, 1)`.
- Ожидание: `TASK_SWAP_MS + 30` = **830 ms**.

### 8.2. Заглушка «Выполнить» — `runTaskExecuteStubFlow()`

| Этап | Длительность |
|------|--------------|
| Показ оверлея (fade in) | 300 ms (`opacity` ease) |
| Задержка действий | 300 ms → закрытие шторы, скролл, `activateTask2RewardReady` |
| Показ картинки | +300 ms fade |
| Удержание | 1200 ms |
| Скрытие | 300 ms fade |

### 8.3. Бейдж шторы → «Заберите награду» — `animateTaskSheetBadgeToRewardReady()`

1. `.is-badge-animating`: fade out иконки и даты — **400 ms**.
2. Смена контента, `.is-reward-ready`, текст даты **`#ffffff`**.
3. Fade in даты — **400 ms**.
4. Кнопка: «Забрать награду».

В шторе задания для партнёрского задания (id `2`) дополнительно пункт «Участие в розыгрыше» (билет); термин награды — **монеты**, подпись «Зачислим сразу».

### 8.4. Получение награды — `claimTask2Reward()`

**Последовательность:**

1. (Опционально) закрытие шторы — **300 ms**.
2. Полёт монет от иконки награды.
3. На **775 ms** (`halfway`): старт `animateTaskCardRemove`.
4. На **80%** полёта первой монеты: `animateCoinBadgeCounter(+250)` за **800 ms**.
5. Исчезновение карточки:
   - `.is-claim-removing`: `task-card-claim-hide` **300 ms**, `cubic-bezier(0.2, 0, 0.4, 0.9)`, scale 1 → 0.8, opacity 1 → 0.
   - `.is-claim-collapsing-out`: height + margin-bottom → 0, **300 ms**, тот же easing.

---

## 9. Полёт монет — `runCoinsToBadgeAnimation()`

Общая функция для награды за слово, задание и любых источников.

### 9.1. Фаза burst (350 ms)

- Монеты: **5–7** шт., размер **24×24 px** (`COIN_FLY_SRC`).
- Разлёт по кругу с jitter угла ±0.275 rad, дистанция **36 px** × (0.85–1.2).
- Easing позиции: `1 − (1−t)³`.

### 9.2. Фаза полёта к бейджу (1200 ms на монету)

- Старт каждой следующей монеты: **+24 ms** stagger.
- Траектория: квадратичная Bézier; контрольная точка выше середины.
- **Scale:** 1 → 0.5.
- **Opacity:** `flyOpacity(t)` — 1 до 40% пути, затем fade.
- Easing: `1 − (1−t)³`.

### 9.3. Промисы анимации

| Промис | Момент |
|--------|--------|
| `firstArrival` | 80% полёта **первой** монеты → старт счётчика |
| `halfway` | 50% от полного цикла (775 ms) → удаление карточки задания |

---

## 10. Прочие UI-анимации

| Элемент | Длительность | Параметры |
|---------|--------------|-----------|
| `.main__fade` (скролл) | 200 ms | opacity, visibility |
| `.raffle-page.is-scrolled .main__fade` | 200 ms | fade при скролле страницы «Задания» |
| `.result-sheet__gradient` | — | `linear-gradient(180deg, rgba(14,14,14,0.25) 0%, #1c1c1e 100%)`, height **200 px**, у штор результата и заданий |
| Кнопка «Играть ещё» active | 120 ms | background |
| Raffle reset / profile switches | 200 ms | background |
| Win-switch toggle | 200 ms | transform |
| `.raffle-event-card__btn` background | 400 ms | `cubic-bezier(0.2, 0, 0.4, 0.9)` |
| `.raffle-event-card__btn-text` color | 400 ms | то же |
| `.raffle-event-card__btn-icon` opacity | 400 ms | то же |
| Tab bar / прочие мелкие active | 120 ms | background |

---

## 11. CSS @keyframes — справочник

| Имя | Длительность | Назначение |
|-----|--------------|------------|
| `cell-win-scale` | 1000 ms | Bounce ячеек при победе |
| `cell-pop` | 100 ms | Pop при вводе буквы |
| `shake` | 600 ms | Тряска строки |
| `gift-prize-landing` | 480 ms | Отскок иконки «Подарки» |
| `raffle-card-shimmer` | 380 ms | Блик на кнопке розыгрыша |
| `prize-wiggle-panel-layer` | 500 ms | Покачивание слоя приза |
| `prize-wiggle-overlay` | 500 ms | Покачивание overlay приза |
| `thematic-sticker-attach` | 420 ms | Появление стикера |
| `prize-particle` | 900 ms | Разлёт частиц |
| `task-card-claim-hide` | 300 ms | Исчезновение карточки задания при claim |

---

## 12. Классы-состояния анимации

| Класс | Когда |
|-------|-------|
| `.is-animating-normal-word` | Переход к экрану результата (1000 ms), под сплешем |
| `.scenario-normal-word` | Состояние «миниполе + панель» |
| `.is-animating-win-hero` | Анимация hero (800 ms) |
| `.scenario-win-hero` | Hero виден |
| `.is-animating-reverse-win-hero` | Скрытие hero + unflip (800 ms) |
| `.is-animating-reverse-normal-word` | Возврат к игре (1000 ms) |
| `.is-returning-to-game` | Вторая фаза возврата |
| `.win-progress--animating` | Идёт анимация прогресса |
| `.is-intro-active` | Intro розыгрыша |
| `.win-splash--win` / `--lose` / `--thematic` | Режим сплеша |
| `.win-splash.is-fading-in` / `.is-fading-out` | Fade фрейма сплеша (600 ms, ease-in) |
| `.win-splash.is-content-in` / `.is-content-out` | Появление / исчезновение контента сплеша |
| `.win-splash.is-title-in` | Заголовок и подзаголовок сплеша победы |
| `.win-splash.is-mini-glow` | Свечение жёлтых ячеек мини-поля на сплеше победы |
| `.is-participate-active` | Анимация участия в розыгрыше |
| `.raffle-page--scroll-locked` | Блокировка скролла во время intro |
| `.raffle-cards-track--locked` | Блокировка скролла карусели при participate |
| `.is-reordering` | FLIP-перестановка карточек заданий |
| `.is-claim-removing` / `.is-claim-collapsing-out` | Удаление карточки задания |
| `.is-reward-ready` | Задание №2 готово к получению награды |
| `.task-sheet-open` | Открыта штора задания |
| `.result-sheet.is-open` / `.is-closing` | Штора результата / задания |

---

*Документ актуален для `app.js` v182, `styles.css` v220.*
