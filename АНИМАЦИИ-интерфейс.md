# Анимации «5 букв» — по интерфейсу

Документ описывает анимации приложения: стадии, тайминги, easing и параметры трансформаций.  
Структура и параметры **1 в 1** с техническим документом [`АНИМАЦИИ.md`](АНИМАЦИИ.md); отличие — понятные названия элементов вместо имён из кода.

Источники: `app.js`, `styles.css`.

---

## 1. Сводка констант (app.js)

### 1.1. Игра и экран результата

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `FLIP_STAGGER_MS` | 150 ms | Задержка между переворотами клеток в строке |
| `FLIP_DURATION_MS` | 750 ms | Длительность переворота клетки (игра) |
| `NORMAL_WORD_MS` | 1000 ms | Переход к экрану результата (под сплешем) |
| `WIN_HERO_MS` | 800 ms | Появление/скрытие блока с отгаданным словом |
| `REVERSE_NORMAL_WORD_MS` | 1000 ms | Возврат к игровому экрану |
| `MINI_UNFLIP_FLIP_MS` | 360 ms | Переворот клетки на мини-поле («Играть ещё») |
| `MINI_UNFLIP_WAVE_COUNT` | 10 | Число диагональных волн (ROWS + COLS − 1) |
| `MINI_UNFLIP_WAVE_STAGGER_MS` | ~48.9 ms | Пауза между волнами: (800 − 360) / 9 |
| `WIN_SPLASH_FADE_MS` | 600 ms | Fade-in / fade-out фрейма сплеша (`ease-in`) |
| `WIN_SPLASH_CONTENT_DELAY_MS` | 200 ms | Пауза после fade-in фрейма до контента |
| `WIN_SPLASH_CONTENT_MS` | 700 ms | Появление / масштаб контента сплеша победы |
| `WIN_SPLASH_CONFETTI_AT` | 0.3 | Доля появления контента, на которой стартуют конфетти |
| `WIN_SPLASH_TITLE_AT` | 0.6 | Доля появления контента, на которой стартуют заголовок и подзаголовок |
| `WIN_SPLASH_TITLE_MS` | 700 ms | Появление заголовка / подзаголовка (победа) |
| `WIN_SPLASH_CONTENT_OUT_MS` | 1000 ms | Исчезновение контента сплеша |
| `WIN_SPLASH_SECONDARY_AT` | 0.7 | Доля скрытия контента, на которой стартует следующий сплеш |
| `WIN_SPLASH_RESULT_AT` | 0.7 | Доля скрытия / fade-out, на которой стартует переход к результату |
| `WIN_SPLASH_TASKS_CONTENT_MS` | 1000 ms | Появление заголовка и карточек сплеша «Прогресс заданий» |
| `WIN_SPLASH_TASKS_PROGRESS_AT` | 0.9 | Доля появления карточек, на которой стартует пробег искры по прогрессу |
| `WIN_SPLASH_TASKS_PROGRESS_DELAY_MS` | 500 ms | Пауза после появления карточек до кнопки «Забрать награду» |
| `WIN_SPLASH_TASKS_PROGRESS_MS` | 1000 ms | Заполнение прогресса / счётчики / кнопка |
| `WIN_SPLASH_TASKS_SWEEP_MS` | 500 ms | Пробег искры по жёлтой полосе до старта роста |
| `WIN_SPLASH_TASKS_SHIMMER_DELAY_MS` | 1000 ms | Пауза после перекраски награды до первого шиммера |
| `WIN_SPLASH_TASKS_SHIMMER_EVERY_MS` | 4000 ms | Период шиммера на прогрессе и блоке награды |
| `LOSE_SPLASH_FLIP_DELAY_MS` | 100 ms | Пауза перед переворотом ответа на сплеше поражения |
| `PROGRESS_PHASE1_MS` | 280 ms | Заполнение линии между кружками прогресса |
| `PROGRESS_PHASE2_MS` | 420 ms | Заполнение кружка прогресса |
| `PROGRESS_PHASE3_MS` | 380 ms | Заполнение линии после кружка |
| `PROGRESS_WORD_DOTS_AT` | 0.05 | Старт анимации точек (доля от total) |
| `PROGRESS_WORD_DOTS_FOR` | 0.25 | Длительность точек (доля от total) |
| `PROGRESS_WORD_DONE_AT` | 0.95 | Конец появления слова (доля от total) |
| `PRIZE_TO_TAB_MS` | 1200 ms | Полёт приза / монет к цели |
| `PROGRESS_SWAP_MS` | 1200 ms | Смена полосы прогресса (сценарий «5 слово») |
| `FIFTH_WORD_PRIZE_LINE_MS` | 420 ms | Заполнение линии к призу (5 слово) |
| `PRIZE_FLY_DELAY_MS` | 1000 ms | Пауза перед полётом приза |
| `PRIZE_POP_SCALE` | 1.2 | Масштаб приза при pop |
| `PRIZE_REVEAL_MS` | 700 ms | Появление приза на новой полосе |
| `PRIZE_WIGGLE_INTERVAL_MS` | 7000 ms | Интервал idle-покачивания приза |
| `RESULT_SHEET_OPEN_MS` | 500 ms | Открытие шторы результата |
| `RESULT_SHEET_CLOSE_MS` | 300 ms | Закрытие шторы |
| `CONFETTI_COUNT` | 200 | Число частиц конфетти |

### 1.2. Призы — карусель карточек розыгрыша и intro

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `RAFFLE_CARD_WIDTH` | 270 px | Ширина карточки розыгрыша |
| `RAFFLE_CARD_HEIGHT` | 388 px | Высота карточки розыгрыша |
| `RAFFLE_CARD_GAP` | 20 px | Зазор между карточками |
| `RAFFLE_SLIDE_STEP` | 290 px | Шаг слайда (width + gap) |
| `RAFFLE_SLIDE_INDICES` | `[0, 1, 2]` | Три слайда **без зацикливания** |
| `RAFFLE_INTRO_ENTRY_DELAY_MS` | 400 ms | Задержка перед intro |
| `RAFFLE_INTRO_STAGE1_MS` | 900 ms | Intro: сборка к центру |
| `RAFFLE_INTRO_PAUSE_MS` | 400 ms | Intro: пауза между стадиями |
| `RAFFLE_INTRO_STAGE2_MS` | 1200 ms | Intro: раскладка карусели |
| `RAFFLE_PARTICIPATE_SCALE` | 1.05 | Масштаб карточки при участии |
| `RAFFLE_PARTICIPATE_SCALE_DELAY_MS` | 200 ms | Задержка перед scale |
| `RAFFLE_PARTICIPATE_SCALE_MS` | 600 ms | Анимация scale/выравнивания |
| `RAFFLE_PARTICIPATE_FLIP_SLOW_MS` | 500 ms | Первый пол-оборот (0° → 180°) |
| `RAFFLE_PARTICIPATE_FLIP_FAST_MS` | 1200 ms | Второй пол-оборот (180° → 360°) |
| `RAFFLE_PARTICIPATE_PAUSE_MS` | 400 ms | Пауза перед возвратом |
| `RAFFLE_PARTICIPATE_RETURN_MS` | 300 ms | Возврат карточки в позицию карусели |
| `RAFFLE_PARTICIPATE_SPARKS_AT_MS` | 150 ms | Искры (50% от return) |
| `RAFFLE_BTN_TRANSITION_MS` | 400 ms | Смена иконки кнопки «Участвовать» |
| `RAFFLE_CENTER_ROTATIONS` | [+1°, −2°, 0°] | Поворот центральных карточек |
| `RAFFLE_TOKEN_SRC` | `Монетка активная.png` | Иконка на активной кнопке |
| `RAFFLE_TOKEN_INACTIVE_SRC` | `Монетка неактивная.png` | Иконка при нехватке монет |
| `COIN_FLY_SRC` | `badge-coin.png?v=2` | Бейдж / летящие монеты |

**Геометрия блока карусели:**

| Элемент | Размер / позиция |
|---------|------------------|
| Блок карусели | height **494 px**, padding-bottom **16 px**; вкладка «Призы» → «Можно выиграть» |
| Карусель и дорожка | height **486 px**; `touch-action: pan-x pan-y` |
| Точки-индикатор (пагинация) | `bottom: 16 px` |
| Подпись «Подведём итоги 1 декабря» над точками | `bottom: 42 px` (18 px над пагинацией) |

**Старт / края:** первая карточка (слайд 0); перелистнуть за 1↔3 нельзя.

### 1.3. Монеты и задания

| Константа | Значение | Назначение |
|-----------|----------|------------|
| `INITIAL_COIN_BALANCE` | 0 | Стартовый баланс монет |
| `WORD_GUESS_COIN_REWARD` | 10 | Награда за угаданное слово (монеты начисляются всегда) |
| `TASK_REWARD_COIN_AMOUNT` | 250 | Награда за задание №2 |
| `COIN_FLY_SIZE` | 24 px | Размер летящей монеты |
| `COIN_COUNTER_START_PROGRESS` | 0.8 | Старт счётчика на 80% полёта первой монеты |
| `TASK_COIN_BURST_MS` | 350 ms | Фаза разлёта монет от источника |
| `TASK_COIN_BURST_DISTANCE` | 36 px | Базовый радиус разлёта (×0.85–1.2 jitter) |
| `TASK_COIN_COUNT_MIN` / `MAX` | 5 / 7 | Число монет в полёте |
| `TASK_SWAP_MS` | 800 ms | FLIP-перестановка карточек заданий |
| `TASK_BADGE_MS` | 400 ms | Смена бейджа в шторе задания |
| `TASK_CLAIM_MS` | 300 ms | Исчезновение карточки при «Забрать награду» |
| `TASK_CLAIM_REORDER_MS` | 300 ms | Схлопывание пункта в списке заданий |
| `TASK_STUB_FADE_MS` | 300 ms | Fade заглушки «Выполнить» |
| `TASK_STUB_HOLD_MS` | 1200 ms | Пауза с картинкой заглушки |
| `TASK_STUB_ACTION_DELAY_MS` | 300 ms | Задержка перед закрытием шторы и активацией награды |

**Производные тайминги полёта монет:**

- Полный полёт: `TASK_COIN_BURST_MS + PRIZE_TO_TAB_MS` = **1550 ms**
- Середина полёта (`halfway`): **775 ms** — старт удаления карточки задания
- Старт счётчика: burst + `COIN_COUNTER_START_PROGRESS × PRIZE_TO_TAB_MS` = 350 + 960 = **1310 ms** (первая монета)
- Счётчик баланса (`animateCoinBadgeValue`): **800 ms**, easing `1 − (1−t)³`

---

## 2. Игровое поле

### 2.1. Ввод буквы — pop клетки

- **Триггер:** в клетку введена буква.
- **Длительность:** 100 ms, `ease`.
- **Ключевые кадры:** scale 1 → 1.04 (50%) → 1.

### 2.2. Переворот клетки при проверке слова — `revealRow()`

**Стадии (на каждую клетку в строке):**

1. Задержка старта: `col × 150 ms` (слева направо).
2. Переворот внутренней стороны клетки.
3. CSS-transition: **750 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`, `rotateY(180deg)`.
4. После завершения всех клеток строки — обновление клавиатуры + **120 ms** пауза до разблокировки ввода.

### 2.3. Ошибка дубликата слова — тряска строки

- **Триггер:** введено слово, которое уже отгадывали.
- **Длительность:** 600 ms, `ease`.
- **Движение:** `translateX` −4px / +4px (чередование).
- Сообщение об ошибке снимается через **600 ms**.

### 2.4. Победа в строке — пульсация клеток

- **Триггер:** все клетки выигрышной строки.
- **Длительность:** 1000 ms, `ease`.
- **Задержка:** `calc(номер_клетки × 100 ms)` — волна слева направо.
- **Ключевые кадры:** 1 → 1.15 (30%) → 1.05 (40%) → 1.1 (50%) → 1.
- **Общая длительность JS-ожидания:** `(COLS − 1) × 100 + 1000` = **1400 ms**.

### 2.5. Подсказка дубликата

- Появление/скрытие: **200 ms**, `opacity` + `visibility` + `transform`.
- Автоскрытие: **1800 ms** (по умолчанию).

---

## 3. Клавиатура

### 3.1. Смена состояния клавиши

- `background`, `border-color`: **380 ms** `ease-out`.
- `color`: **200 ms** `ease-out`.

### 3.2. Нажатие

- `transform: scale(0.96)` — **80 ms** (мгновенный active).

---

## 4. Экран результата и сплеши

### 4.1. Общая последовательность

**Победа:**

```
переворот строки → пульсация клеток (1400 ms)
→ сплеш победы (фрейм + контент; под сплешем — переход к результату 1000 ms)
→ [если «Тематическое слово»] сплеш тематического слова
   ИНАЧЕ [если «Сплеш уведомлений»] сплеш уведомлений
→ [если «Прогресс заданий»] сплеш прогресса заданий
→ исчезновение фрейма сплеша (600 ms; блок результата стартует на 70% fade)
→ появление блока с отгаданным словом (800 ms, параллельно с прогрессом)
```

**Поражение (6 попыток):**

```
переворот строки → сплеш поражения (фрейм + контент + переворот ответа;
  под сплешем — переход к результату 1000 ms)
→ [если «Тематическое слово»] сплеш тематического слова
→ [если «Прогресс заданий»] сплеш прогресса заданий
→ исчезновение фрейма сплеша (600 ms; блок результата на 70% fade)
→ появление блока результата (800 ms, без крупного слова победы)
```

**Цепочка follow-up:** следующий сплеш стартует на **70%** скрытия контента предыдущего.  
**К результату:** скрытие контента последнего сплеша ждётся **70%**, затем fade фрейма; блок результата стартует на **70%** fade.  
«Тематическое слово» и «Сплеш уведомлений» взаимно исключают друг друга. «Прогресс заданий» независим и всегда последний.  
Сплеш уведомлений после поражения **не** показывается.

**Easing контента сплешей:** `cubic-bezier(0.25, 1.4, 0.65, 1.3)`.  
**Easing заполнения прогресса / кнопки на сплеше заданий:** `cubic-bezier(0.45, 1.45, 0.8, 1)`.  
**Фрейм сплеша:** от верхней границы строки навигации до низа, фон `#1C1C1E`.  
Кнопка «Закрыть» остаётся над сплешем и кликабельна.

---

### 4.2. Сплеш победы — `playWinSplashAnimation()`

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Появление фрейма | 600 ms | `ease-in` | Прозрачность фрейма 0 → 1 |
| Пауза | 200 ms | — | Пустой фрейм |
| Появление контента | 700 ms | `cubic-bezier(0.25, 1.4, 0.65, 1.3)` | Картинка `Verh.png` 260×200: 0→1, масштаб 1.2→1.0. Кнопки 0→1. Параллельно переход к мини-полю |
| Конфетти | старт на **30%** появления (= 210 ms) | — | Радиальный burst из центра картинки |
| Заголовок и подзаголовок | старт на **60%** (= 420 ms) | 700 ms | 0→1; отступ copy 24→8; подзаголовок 28→12; gap картинка→текст 4 px |
| «Пропустить» | 1000 ms (ожидание **70%** при follow-up / к результату) | bezier | Контент 1→0; отступы обратно |
| Исчезновение фрейма | 600 ms | `ease-in` | Блок результата на **70%** |

---

### 4.3. Сплеш поражения — `playLoseSplashAnimation()`

Без конфетти.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Появление фрейма | 600 ms | `ease-in` | 0 → 1 |
| Пауза | 200 ms | — | Пустой фрейм |
| Появление контента | 700 ms | bezier | «Не отгадали слово», «Мы загадали:», 5 клеток, «Коснитесь…». Параллельно переход к результату |
| Переворот ответа | через 100 ms | как в игре | 150 ms × колонка + 750 ms; жёлтые клетки с загаданным словом |
| Тап | 1000 ms (ожидание **70%** при follow-up / к результату) | bezier | Контент 1 → 0 |
| Исчезновение фрейма | 600 ms | `ease-in` | После thematic / tasks; результат на **70%** |

---

### 4.4. Сплеш тематического слова — `playThematicSplashAnimation()`

Паузы перед контентом нет.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Появление | 1000 ms | `cubic-bezier(0.25, 1.4, 0.65, 1.3)` | Блок 0→1; картинка 0.9→1.0; отступы слово↔картинка и картинка↔подзаголовок **24→4** |
| «Пропустить» | 1000 ms (ожидание **70%**) | bezier | 1→0; отступы **4→24** |

---

### 4.5. Сплеш уведомлений — `playNotifySplashAnimation()`

Только после **победы**.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Появление | 1000 ms | `cubic-bezier(0.25, 1.4, 0.65, 1.3)` | `Uvedi.png`; заголовок 24→8; подзаголовок 28→12 (как на победе); switch + «Продолжить» |
| «Продолжить» | 1000 ms (ожидание **70%**) | bezier | 1→0; отступы обратно |

Switch без логики.

---

### 4.6. Сплеш прогресса заданий — `playTasksProgressSplashAnimation()`

Последний в цепочке.

| Стадия | Длительность | Easing | Что происходит |
|--------|--------------|--------|----------------|
| Появление | 1000 ms | `cubic-bezier(0.25, 1.4, 0.65, 1.3)` | «Прогресс заданий»; карточки без наклона; gaps 64→32 / 48→20 |
| Искра по прогрессу | на **90%** появления, **500 ms** | linear | По жёлтой полосе с вылетающими искрами → затем рост |
| Заполнение | 1000 ms | `cubic-bezier(0.45, 1.45, 0.8, 1)` | Полосы 8 px; roll; у 2-й карточки — «Выполнено» + жёлтая награда |
| Кнопка | после 1000 + 500 ms | 1000 ms | «Забрать награду» |
| Шиммер | через 1000 ms после перекраски | каждые **4000 ms** | 20°, ширина ×2, белый 0.4 |
| Скрытие | 1000 ms (ожидание **70%**) | bezier | Gaps 32→64 / 20→48; бейджи остаются |
| Исчезновение фрейма | 600 ms | — | Результат на **70%** |

**«Забрать награду»:** +250 монет к бейджу на сплеше.

---

### 4.7. Переход к результату — `playNormalWordAnimation()`

**1000 ms**, параллельно появлению сплеша победы/поражения. Сетка → мини-поле 23×22, gap 4; клавиатура вниз; панель результата вверх.

---

### 4.8. Блок с отгаданным словом — `playWinHeroAnimation()`

**800 ms**, старт на **70%** исчезновения фрейма сплеша. Параллельно полоса прогресса / «5 слово».

---

### 4.9. Конфетти — `playConfetti()` / `playSplashConfetti()`

- 200 частиц; цвета `#ffdd2d`, `#19b1ff`, `#7d3bc0`, `#ffb400`.
- Сплеш победы: радиальный burst на **30%** появления контента.
- Остальные сплеши: без конфетти.

---

### 4.10. «Играть ещё» — обратные анимации

#### 4.10.1. Скрытие блока с отгаданным словом — 800 ms

Скролл наверх, скрытие блока, переворот мини-поля (см. §4.11).

#### 4.10.2. Возврат к игровому экрану — 1000 ms

Easing `cubic-bezier(0.7, 0, 0.3, 1)`.

---

### 4.11. Переворот мини-поля обратно — `unflipAllCells()`

**800 ms**, 10 диагональных волн; переворот клетки **360 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`.

---

## 5. Полоса прогресса

### 5.1. Общая длительность фаз по слоту

| Слот | Phase 1 | Phase 2 | Phase 3 | **Total** |
|------|---------|---------|---------|-----------|
| 0 | — | 420 ms | 380 ms | **800 ms** |
| 1–3 | 280 ms | 420 ms | 380 ms | **1080 ms** |

### 5.2. Фазы анимации прогресса `animateWinProgress(word)`

1. **Phase 1** (слоты 1–3): линия до кружка → 100%, **280 ms**.
2. **Phase 2:** заливка кружка (scaleX 0→1), **420 ms**. Опционально: тематический стикер; при включённом разделе розыгрышей — полёт монет + счётчик **+10**.
3. **Phase 3:** линия после кружка → 50%, **380 ms**.

**CSS transitions при анимации полосы:**

| Элемент | Быстрый | Медленный (медленная фаза) |
|---------|---------|----------------------------|
| Заливка линии между кружками | 280 ms | 380 ms |
| Заливка кружка | 280 ms | — |
| Цифра на кружке (color) | 420 ms | — |
| Заливка «ножки» приза | 280 ms / 420 ms | — |

Easing фаз прогресса: `cubic-bezier(0.45, 1.45, 0.8, 1)`.

### 5.3. Награда монетами за слово — `triggerWinProgressCoinReward()`

**Условие:** всегда (вкладка «Задания» и бейдж монет не зависят от «Активная стадия»).

1. Полёт монет к счётчику монет от кружка прогресса (или от точки приза в сценарии «5 слово»).
2. После `firstArrival` (80% полёта первой монеты) — крутилка счётчика на **+10** за **800 ms**.
3. Параллельно с phase 2/3 прогресса, не блокирует анимацию полосы.

### 5.4. Анимация слова под кружком

- **Точки:** старт на **5%** total, длительность **25%** total.
- **Слово:** старт после точек, видимость до **95%** total.
- Easing: `cubic-bezier(0.45, 1.45, 0.8, 1)`.
- Начальное состояние: `opacity: 0`, `scale(0.8)`.

### 5.5. Тематический стикер — `thematic-sticker-attach`

- **420 ms**, `cubic-bezier(0.45, 1.45, 0.8, 1)`.
- От `opacity: 0`, `scale(0.35)`, `rotate(-14°)` → к финальному состоянию.

### 5.6. Приз на полосе прогресса

#### Появление (reveal)

- **700 ms**, `cubic-bezier(0.33, 1, 0.68, 1)`.
- `opacity: 0 → 1`, `scale: 0.5 → 1`.

#### Pop (пятисловный сценарий)

- Состояние «всплеск»: **scale 1.2**, **420 ms**, `cubic-bezier(0.33, 1, 0.68, 1)`.
- Частицы: `spawnPrizeParticles(el, 1.7)`.

#### Idle-покачивание — `wigglePrize()`

- Интервал: **7000 ms**.
- Анимация: **500 ms**, `ease-in-out`.

#### Полёт к вкладке «Призы» — `flyPrizeToGiftIcon()`

- **1200 ms**, квадратичная Bézier.
- **Scale:** 1.2 → 0.5.
- **Opacity:** 1 до 40% пути, затем fade (`flyOpacity`).
- Easing позиции: `1 − (1−t)³`.
- Иконка вкладки «Призы»: отскок при приземлении, **480 ms**, срабатывает на **60%** полёта.

### 5.7. Сценарий «5 слово» — `animateFifthWordProgress()`

1. Анимация слова «ВКЛАД» в колонке приза.
2. Линия 3 → 100% (**420 ms** slow).
3. Коннектор → 100%, заливка «ножки» приза → 1.
4. Полёт монет **+10** от точки приза (как в §5.3).
5. Pop приза + частицы.
6. Пауза **1000 ms** → полёт к вкладке «Призы» (**1200 ms**).
7. Смена полосы прогресса (**1200 ms**) → появление новой (**700 ms**).

---

## 6. Штора результата

### 6.1. Открытие

- Панель шторы: `translateY(100%) → 0`, **500 ms**, `cubic-bezier(0.4, 0.1, 0.2, 1)`.
- Затемнение фона: `opacity 0 → 1`, **500 ms**.

### 6.2. Закрытие (крестик / drag)

- **300 ms**, `cubic-bezier(0.4, 0, 1, 1)`.
- Состояние закрытия.

### 6.3. Drag-to-close

- Порог: **25%** высоты панели.

Штора задания использует те же `RESULT_SHEET_OPEN_MS` / `RESULT_SHEET_CLOSE_MS` и ту же механику drag.

---

## 7. Карусель призов (вкладка «Призы» → «Можно выиграть»)

Карусель на экране призов. **«Активная стадия»** вкл. — карусель; выкл. — заглушка Hero (вкладка «Задания» и монеты остаются). Intro/участие — при открытии «Призы», если стадия активна.

### 7.1. Карусель — `updateCardTransforms()`

Интерполяция по `progress` = `min(1, |offset| / RAFFLE_SLIDE_STEP)` (0 = центр, 1 = бок):

| Параметр | Центр (0) | Бок (1) |
|----------|-----------|---------|
| `scale` (обёртка карточки) | 1 | 0.9 |
| `translateY` | 0 px | 48 px |
| `opacity` | 1 | 0.75 |
| `rotate` | centerRotate × (1−p) + offset/step × 4° | — |
| `scale` (картинка на карточке) | 1 | 0.9 |

**centerRotate:** `[+1°, −2°, 0°]` по индексу карточки.

Обновление: `scroll` (RAF) + `scrollend` (debounce **120 ms**). Слайды `0…2` без лупа.

### 7.2. Подпись над пагинацией — `updateRafflePaginationLabelOpacity()`

Текст «Подведём итоги 1 декабря»:

```
distanceFromSnap = |scrollProgress − round(scrollProgress)|
opacity = 1 − min(1, distanceFromSnap × 2)
```

- Карточка в центре snap → **opacity 1**.
- Между snap-позициями (середина скролла) → **opacity 0**.
- Пересчёт на каждом кадре скролла (без CSS-transition).
- Во время intro (`is-intro-active`, `raffleIntroRunning`) — управляется отдельно (см. §7.5).

### 7.3. Переворот карточки тапом — `toggleRaffleCardFlip()`

- **750 ms**, `cubic-bezier(0.4, 0, 0.15, 1)`.
- Накопительный `rotateY` (+180° за тап).

### 7.4. Участие в розыгрыше — `runRaffleParticipate()`

| # | Стадия | Длительность | Easing / параметры |
|---|--------|--------------|-------------------|
| 1 | Задержка | 200 ms | — |
| 2 | Scale + выравнивание | 600 ms | `cubic-bezier(0, 0, 0.4, 0.9)`, scale **1.05** |
| 3 | Flip 0° → 180° | 500 ms | `cubic-bezier(0.4, 0, 1, 1)` |
| 4 | Смена контента | — | — |
| 5 | Flip 180° → 360° | 1200 ms | `cubic-bezier(0.4, 0.1, 0.2, 1)` |
| 6 | Пауза | 400 ms | — |
| 7 | Возврат в карусель | 300 ms | `cubic-bezier(0.45, 1.45, 0.48, 1)` |
| 8 | Искры | 150 ms от старта return | `particleScale: 4.05`, sparkOnly |

**Параллельно со стадией 1:** списание монет и крутилка счётчика (**800 ms**, без ожидания).

**Кнопка «Участвовать»:** иконки `Монетка активная` / `Монетка неактивная`; смена — **400 ms**, opacity crossfade через **200 ms**. Во время intro кнопки тоже отражают баланс.

### 7.5. Intro при первом входе — `runRaffleIntroAnimation()`

**Задержка:** **400 ms**. Старт — слайд **0** (первая карточка).

**Начальное состояние:** центр экрана, `scale: 0.8`, `opacity: 0`; карточка 0 — flip **180°**.

#### Стадия 1 — 900 ms

- Easing: `cubic-bezier(0, 0, 0.4, 0.9)`.
- Карточка 0: `opacity → 1`, flip → 0°.

#### Пауза — 400 ms

#### Стадия 2 — 1200 ms

- Easing: `cubic-bezier(0.35, 1.3, 0.25, 1)`.
- Раскладка в позиции карусели (слайды 0–2 без клонов).
- **Точки-индикатор и подпись «Подведём итоги 1 декабря»:** `opacity 0 → 1`, transition **1200 ms**, тот же easing.

**Итого:** 400 + 900 + 400 + 1200 = **2900 ms**.

### 7.6. Shimmer на кнопке «Участвовать»

- **380 ms**, `ease-out`.
- Градиентная полоса: `translate(-55%, 55%)` → `translate(55%, -55%)`.

---

## 8. Задания

### 8.1. Перестановка карточек — `animateTaskCardsSwap()`

- FLIP: начальный `translateY(delta)` без transition → сброс transform.
- Transition: **800 ms**, `cubic-bezier(0.4, 0.1, 0.2, 1)`.
- Ожидание: `TASK_SWAP_MS + 30` = **830 ms**.

### 8.2. Заглушка «Выполнить» — `runTaskExecuteStubFlow()`

| Этап | Длительность |
|------|--------------|
| Показ тёмного оверлея на игре (fade in) | 300 ms (`opacity` ease) |
| Задержка действий | 300 ms → закрытие шторы, скролл, активация «Заберите награду» |
| Показ картинки-заглушки | +300 ms fade |
| Удержание | 1200 ms |
| Скрытие | 300 ms fade |

### 8.3. Бейдж шторы → «Заберите награду» — `animateTaskSheetBadgeToRewardReady()`

1. Анимация бейджа: fade out иконки и даты — **400 ms**.
2. Смена контента, состояние «награда готова», текст даты **`#ffffff`**.
3. Fade in даты — **400 ms**.
4. Кнопка: «Забрать награду».

В шторе для партнёрского задания (Магнит) — пункт «Участие в розыгрыше» (билет); награды — **монеты**, «Зачислим сразу».

### 8.4. Получение награды — `claimTask2Reward()`

**Последовательность:**

1. (Опционально) закрытие шторы задания — **300 ms**.
2. Полёт монет от иконки награды на карточке.
3. На **775 ms** (`halfway`): старт исчезновения карточки задания.
4. На **80%** полёта первой монеты: крутилка счётчика (+250) за **800 ms**.
5. Исчезновение карточки:
   - Состояние «удаление»: анимация исчезновения **300 ms**, `cubic-bezier(0.2, 0, 0.4, 0.9)`, scale 1 → 0.8, opacity 1 → 0.
   - Состояние «схлопывание»: height + margin-bottom → 0, **300 ms**, тот же easing.

---

## 9. Полёт монет — `runCoinsToBadgeAnimation()`

Общая функция для награды за слово, задание и любых источников.

### 9.1. Фаза burst (350 ms)

- Монеты: **5–7** шт., размер **24×24 px**.
- Разлёт по кругу с jitter угла ±0.275 rad, дистанция **36 px** × (0.85–1.2).
- Easing позиции: `1 − (1−t)³`.

### 9.2. Фаза полёта к счётчику монет (1200 ms на монету)

- Старт каждой следующей монеты: **+24 ms** stagger.
- Траектория: квадратичная Bézier; контрольная точка выше середины.
- **Scale:** 1 → 0.5.
- **Opacity:** `flyOpacity(t)` — 1 до 40% пути, затем fade.
- Easing: `1 − (1−t)³`.

### 9.3. Промисы анимации

| Промис | Момент |
|--------|--------|
| `firstArrival` | 80% полёта **первой** монеты → старт счётчика |
| `halfway` | 50% от полного цикла (775 ms) → удаление карточки задания |

---

## 10. Прочие UI-анимации

| Элемент | Длительность | Параметры |
|---------|--------------|-----------|
| Градиентный fade при скролле основной области | 200 ms | opacity, visibility |
| Fade при скролле страницы «Задания» | 200 ms | opacity |
| Градиент верха шторы (результат / задания) | — | `rgba(14,14,14,0.25) → #1c1c1e`, height 200 px |
| Кнопка «Играть ещё» active | 120 ms | background |
| Сброс прогресса заданий / переключатели профиля | 200 ms | background |
| Ползунок переключателя в профиле | 200 ms | transform |
| Фон кнопки «Участвовать» на карточке | 400 ms | `cubic-bezier(0.2, 0, 0.4, 0.9)` |
| Цвет текста кнопки «Участвовать» | 400 ms | то же |
| Прозрачность иконки кнопки «Участвовать» | 400 ms | то же |
| Нижняя панель вкладок / прочие мелкие active | 120 ms | background |

---

## 11. CSS @keyframes — справочник

| Анимация | Длительность | Назначение |
|----------|--------------|------------|
| Пульсация клетки при победе | 1000 ms | Bounce клеток при победе |
| Pop при вводе буквы | 100 ms | Pop при вводе буквы |
| Тряска строки | 600 ms | Тряска строки при дубликате |
| Отскок иконки «Призы» | 480 ms | Отскок иконки «Призы» |
| Блик на кнопке «Участвовать» | 380 ms | Блик на кнопке «Участвовать» |
| Покачивание слоя приза | 500 ms | Покачивание слоя приза |
| Покачивание overlay приза | 500 ms | Покачивание overlay приза |
| Появление тематического стикера | 420 ms | Появление стикера |
| Разлёт частиц приза | 900 ms | Разлёт частиц |
| Исчезновение карточки задания при claim | 300 ms | Исчезновение карточки при «Забрать награду» |

---

## 12. Состояния анимации

| Состояние | Когда |
|-----------|-------|
| Анимация перехода к экрану результата | Переход к экрану результата (1000 ms), под сплешем |
| Режим мини-поля | Мини-поле + панель результата |
| Анимация блока с отгаданным словом | Появление блока слова (800 ms) |
| Блок с отгаданным словом виден | Блок слова отображается |
| Обратная анимация блока слова | Скрытие блока + переворот мини-поля (800 ms) |
| Обратная анимация мини-поля | Возврат к игре (1000 ms) |
| Вторая фаза возврата к игре | Вторая фаза возврата |
| Анимация полосы прогресса | Идёт анимация прогресса |
| Intro карусели призов | Intro карусели на вкладке «Призы» |
| Сплеш победы / поражения / thematic / notify / tasks | Режим сплеша |
| Fade фрейма сплеша | Появление / исчезновение фрейма (600 ms, ease-in) |
| Контент сплеша виден / скрывается | Появление / исчезновение контента сплеша |
| Заголовок сплеша победы | Заголовок и подзаголовок сплеша победы |
| Прогресс на сплеше заданий | Заполнение полос / счётчиков |
| Кнопка награды на сплеше заданий | Появление «Забрать награду» |
| Карточка задания выполнена | Жёлтый блок награды |
| Шиммер на сплеше заданий | Блик по прогрессу и награде каждые 4 с |
| Анимация участия в розыгрыше | Анимация участия в карточке приза |
| Блокировка скролла страницы «Призы» / «Задания» | Блокировка скролла во время intro |
| Блокировка скролла карусели | Блокировка скролла карусели при participate |
| Перестановка карточек заданий | FLIP-перестановка карточек заданий |
| Удаление / схлопывание карточки задания | Удаление карточки задания |
| Задание готово к получению награды | Задание №2 готово к «Забрать награду» |
| Штора задания открыта | Открыта штора задания |
| Штора результата открыта / закрывается | Штора результата / задания |

---

*Документ актуален для `app.js` v242, `styles.css` v283.  
Технический документ с именами из кода: [`АНИМАЦИИ.md`](АНИМАЦИИ.md).*
